[
  {
    "name": "Seleccionar todo",
    "database": "postgres",
    "description": "Una muy breve descripción",
    "query": "SELECT * FROM mytable;"
  },
  {
  "name": "Lorem ipsum dolor sit amet",
  "database": "mongo",
  "description":
  "Vestibulum vulputate metus at nisl lacinia blandit. Quisque fermentum sagittis mi",
  "query": "myCollection.find( {\"attribute\": \"value\"} )"
  },
  {
    "name": "Sebastián",
    "database": "postgres",
    "description": "Una consulta de Sebastián 🙋",
    "query": "SELECT * FROM mytable WHERE nombre='Sebastián';"
  },
  {
    "name": "Quiero todo de mongo 😄",
    "database": "mongo",
    "description": "...",
    "query": "myCollection.find()"
  },
  {
    "name": "Insertar a mongo",
    "database": "mongo",
    "description":
      "Vestibulum vulputate metus at nisl lacinia blandit. Quisque fermentum sagittis mi",
    "query": "myCollection.insert( { \"attribute\": \"value\" } )"
  }
  {
    "name": "Obtener hotel",
    "database": "postgres",
    "description":
      "Recive una fecha x y una cidudad y, y retorna los hoteles en los que se hubo un hospedaje en dicha fecha y ciudad",
    "query": "SELECT Hotel.nombre FROM Hotel,Hospedaje WHERE Hotel.hid=Hospedaje.hid AND Hospedaje.fecha <= x AND Hospedaje.fecha + Hospedaje.dias >= x AND Hotel.cidudad = y;"
  }
  {
    "name": "Obtener restaurant",
    "database": "postgres",
    "description":
      "Recive una fecha x y una cidudad y, y retorna los restaurantes en los que se hubo una reserva en dicha fecha y ciudad",
    "query": "SELECT restaurantes.nombre FROM restaurantes,reservas WHERE restaurantes.id_restoran = reservas.id_restoran AND restaurantes.ciudad = y AND reservas.fecha = x;"
  }
  {
    "name": "Mensajes de una fecha",
    "database": "mongo",
    "description":
      "Recive una fecha x y retorna los numeros de los que tienen mensajes en esa fechas",
    "query": "db.colletionprueba.find({fecha:"x"},{}).pretty()"
  }
  {
    "name": "Ultimos k mensajes de un numero",
    "database": "postgres",
    "description":
      "Recive un numero x y un cantidad k, y muestra los ultimos k mensajes enviados por ese numerodb.colletionprueba.find({fecha:"numero"},{}).limit("k").pretty()",
    "query": "db.colletionprueba.find({fecha:"numero"},{}).limit("k").pretty()"
  }
  {
    "name": "Ultimos k mensajes de un numero",
    "database": "postgres",
    "description":
      "Recive un numero x y un cantidad k, y muestra los ultimos k mensajes enviados por ese numerodb.colletionprueba.find({fecha:"numero"},{}).limit("k").pretty()",
    "query": "db.colletionprueba.find({fecha:"numero"},{}).limit("k").pretty()"
  }
]
